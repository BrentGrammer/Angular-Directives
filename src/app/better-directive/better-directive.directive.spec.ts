import { BetterHighlightDirective } from "./better-directive.directive";

describe("BetterDirectiveDirective", () => {
  it("should create an instance", () => {
    const directive = new BetterHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
